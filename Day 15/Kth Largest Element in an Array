class Solution {
    public int findKthLargest(int[] nums, int k) {
        int count=0;
        Arrays.sort(nums);
        for(int i=nums.length-1;i>=0;i--){
            count++;
            if(count==k){
                return nums[i];
            }

        }
        return -1;
        
    }
}
